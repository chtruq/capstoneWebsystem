/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["firebasestorage.googleapis.com", "example.com", "www.apartments.com", "storage.googleapis.com"],
  },
};

export default nextConfig;
